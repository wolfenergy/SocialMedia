pragma solidity ^0.8.0;

contract SocialMedia {
    struct Post {
        address author;
        string contentHash; // IPFS hash for post content
        uint timestamp;
        uint likes;
    }

    
